name: release

on:
  push:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running lint checks"

  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running unit tests"

  package:
    runs-on: ubuntu-latest
    needs: [lint, unit-tests]
    steps:
      - run: echo "Packaging release"
